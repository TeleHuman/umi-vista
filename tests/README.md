# Tests

This directory will hold smoke tests and validation tests after implementation code is added.

Expected future contents:

- Import and CLI smoke tests.
- Physical-validation scoring tests.
- Simulation evaluation dry-run tests.
- Configuration validation tests.

Current status: placeholder only.
